<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>{{title}}</title>
</head>

<body>
    <div id="wrapper">
        <header>
            <!-- Navigation -->
            <nav>
                <div>
                    <a href="/">Home</a>
                    <a href="/items/catalog">Dashboard</a>
                    <a href="/items/search">Search</a>
                </div>
                {{#if isAuthenticated}}
                <!-- Logged-in users -->
                <div class="user">
                    <a href="/items/create">Add Stones</a>
                    <a href="/auth/logout">Logout</a>
                </div>
                {{else}}
                <!-- Guest users -->
                <div class="guest">
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">Register</a>
                    {{/if}}
                </div>
            </nav>
        </header>

    
        {{#if error}}
        <div>
            <div class="errorContainer">
                <p>{{error}}</p>
            </div>
        </div>
        {{/if}} 
        
        {{{body}}}
    
    <footer>
        <p>@EarthTreasureVaul</p>
    </footer>
</body>

</html>